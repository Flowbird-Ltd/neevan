<?xml version="1.0" encoding="UTF-8"?>
<templates>

	<t t-name="mail.chatter.Buttons">
		<button t-if="logNoteButton" class="btn btn-link o_chatter_button_log_note"
			title="Log a note. Followers will not be notified." disabled="disabled">
			Log note
		</button>
		<button t-if="scheduleActivityButton" class="btn btn-link o_chatter_button_schedule_activity"
			title="Log or schedule an activity" disabled="disabled">
			<i class="fa fa-clock-o" title="Dates" />
			<t t-if="isMobile">Activity</t>
			<t t-else="">Schedule activity</t>
		</button>
	</t>

</templates>